@echo off
powershell -NoProfile -Command "$wd = @(Get-CimInstance Win32_Process | Where-Object { $_.Name -eq 'pythonw.exe' -and $_.CommandLine -match 'watchdog\.py' }); $app = @(Get-CimInstance Win32_Process | Where-Object { $_.Name -eq 'python.exe' -and $_.CommandLine -match 'app\.py' }); if ($wd.Count -eq 0 -and $app.Count -eq 0) { Write-Host '[stop] no watchdog/app running' -ForegroundColor Yellow; exit }; foreach ($p in $wd) { Stop-Process -Id $p.ProcessId -Force -ErrorAction SilentlyContinue; Write-Host ('[stop] watchdog killed PID=' + $p.ProcessId) -ForegroundColor Green }; Start-Sleep 1; foreach ($p in $app) { try { Stop-Process -Id $p.ProcessId -Force -ErrorAction Stop; Write-Host ('[stop] app killed PID=' + $p.ProcessId) -ForegroundColor Green } catch { Write-Host ('[stop] app PID=' + $p.ProcessId + ' already gone') -ForegroundColor DarkGray } }"
pause
