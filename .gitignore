# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# 編輯器 / IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Server 執行期 log（每次跑都會重寫）
server.log
server.err.log

# 舊 CSV 備份（已遷移至 SQLite）
*.csv.migrated

# 注意：wait_log.db 首次 push 當示例資料
# 之後用 `git update-index --skip-worktree wait_log.db` 避免新資料被追蹤
